<!DOCTYPE html>
<html>
  <head>
    <title>QuizApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <%= yield :stylesheets %>
    <link rel="stylesheet" href="https://unpkg.com/aos@next/dist/aos.css" />
    
  </head>

  <body>
      <% if !Current.is_public_schema %>
        <nav class="navbar navbar-expand-lg navbar-dark bg-dark"> <!-- Added "navbar-dark" and "bg-dark" classes -->
          <div class="container">
            <a class="navbar-brand" href="/">Quiz App</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="w-100 collapse navbar-collapse justify-content-center" id="navbarNav">
              <ul class="navbar-nav">
                <li class="nav-item">
                    <a class="nav-link" href="#">Home</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="#">About</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="#">Profile</a>
                </li>
              </ul>
            </div>
            <div class="w-25 d-flex justify-content-end">
              <a class="nav-link bg-danger text-white p-2" href="<%= destroy_user_session_path %>">Sign OUT</a>
            </div>
          </div>
          </div>
        </nav>
      <%end%>
    <%= render "layouts/flash_message" %>
    <div style="margin-bottom: 6rem;">
      <%= yield %>
    </div>
    <footer class="footer bg-dark text-white py-4 fixed-bottom mt-4">
        <div class="container text-center">
            &copy; 2023 Quiz App. All rights reserved.
        </div>
    </footer>
      <script src="https://unpkg.com/aos@next/dist/aos.js"></script>
      <script>
        AOS.init({
          duration: 900
        });
      </script>
  </body>
</html>
