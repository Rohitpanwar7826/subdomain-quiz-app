<%= turbo_stream.replace "quiz-exam-#{@quiz.id}", partial: "form", 
  locals: 
    { 
      quiz: @quiz, 
      question: @question, 
      request_url: @has_next_question ? 
        exam_create_path(@quiz, @question) : 
        exam_final_assessment_path(@quiz, @question)
    }
%>